[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "src"
version = "0.1.0"

[tool.setuptools]
py-modules = ["helpers"]

[tool.setuptools.packages.find]
where = ["."]
